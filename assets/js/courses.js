// email, courseUrl, index
// select courseId from courses where courseURL  = courseURL
function onCheckboxClick(){
    let course = document.querySelector("title");
    let user = getCookie("user");
    
}